<Page x:Class="WpfDokter.Views.PatientenPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Patiënten"
      Loaded="Page_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Content="← Terug naar overzicht"
                HorizontalAlignment="Left"
                Margin="0,0,0,12"
                Padding="12,6"
                Click="BtnTerugNaarStart_Click"/>

        <TextBlock Grid.Row="1"
                   Text="Patiënten"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <!-- Zoeken op voornaam of achternaam -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Zoeken op naam"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>
            <TextBox x:Name="txtZoek"
                     Padding="8,6"
                     MaxWidth="480"
                     HorizontalAlignment="Left"
                     MinWidth="280"
                     TextChanged="TxtZoek_TextChanged"/>
        </StackPanel>

        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <WrapPanel x:Name="pnlKaarten"
                       Orientation="Horizontal"/>
        </ScrollViewer>
    </Grid>
</Page>
